#註解 Command + Shift + C 

#Repositories:
#   1. CRAN (Comprehensive R Archive Network): R’s main repository 
#   2. BioConductor: A repository mainly for bioinformatic-focused packages
#   3. GitHub: A very popular, open source repository (not R specific!)

#Exploring packages
#   1. CRAN’s “Task View” that groups packages into 35 topics
#   2. RDocumentation:for packages and functions from CRAN, BioConductor, and GitHub 

#Instruction
  help(package='ggplot2')
  browseVignettes('ggplot2')
  
  sessionInfo()
# -version of R
# -all loaded packages
# -your OS

# Installing 
# 1.from CRAN
  install.packages("ggplot2") 
  install.packages(c("ggplot2", "devtools", "lme4"))
# 2.from Bioconductor
  BiocManager::install("GenomicFeatures")
# 3.from GitHub
  install_github("author/package")

#Load it
library(ggplot2)

#Check already installed
installed.packages() 
library()

#update
old.packages() #updated packages
update.packages() #update all
install.packages("packagename") #update one

#unload
detach("package:ggplot2", unload=TRUE)

#uninstall
remove.packages("packagename")

#Install and load the KernSmooth R package. What does the copyright message say?
install.packages('KernSmooth')
library(KernSmooth)


